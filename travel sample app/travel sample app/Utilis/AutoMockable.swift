import Foundation

// `AutoMockable` is just a tag interface to generate mocks with Sourcery.
// You can re-generate the mocks executing `rake mocks` on the root of the project,
// where you will file the `Rakefile`.
public protocol AutoMockable { }
